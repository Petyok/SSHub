# Demo runtime state — regenerated on every recording
home/.local/

# Throwaway SFTP demo server: ephemeral keys + curated remote sandbox
# (regenerated by demo/sftp-server.sh on every run)
home/.ssh/
home/sftp-remote/
home/sftp-local/
screenshots/_scratch.gif
