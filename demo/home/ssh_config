# Demo hosts are seeded directly into launcher.db by demo/seed_demo.rs
# (with playful names + real pingable IPs). Intentionally empty so the app
# doesn't merge any ssh_config hosts on top of the seeded ones.
